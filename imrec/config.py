"""界面与算法用到的常量。"""

# 原图 / 输出 / 拟合 / 热力图 使用相同最长边缩放（滚动区可查看全图）
PREVIEW_MAX_SIDE = 400

# 四宫格内各图最小尺寸（一致比例区域由布局均分）
IMG_CELL_MIN = (240, 200)

# 热力图渐变端点（BGR）：少=蓝，多=红（线性插值，中间偏紫）
HEATMAP_BGR_BLUE = (255, 0, 0)
HEATMAP_BGR_RED = (0, 0, 255)
